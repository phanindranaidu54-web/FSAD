
FSAD SKILL 03
Working with HQL – Sorting, Pagination & Aggregates

Files:
Product.java – Entity class
HQLExample.java – HQL query examples

Main Operations Demonstrated:
1. Sorting using ORDER BY
2. Pagination using setFirstResult() and setMaxResults()
3. Aggregate functions COUNT, MIN, MAX
4. GROUP BY operations
5. WHERE filtering with price range
6. LIKE pattern matching

Git Commands:
git init
git add .
git commit -m "Skill 3 HQL Implementation"
git remote add origin <repo_url>
git push -u origin main
